
Session variables:
    s_user_id     the id of the user who is logged in
    s_applicant   true iff the logged-in user is an applicant
    s_recruiter   true iff the logged-in user is a recruiter
    s_admin       true iff the logged-in user is an administrator

